---
layout: default
title: ima progrmr
---

.meta
  = link_to('about me', 'http://absurd.li')

- current_year = nil    # ugly, but good enough
- group_posts_by_month_and_day(site.posts) do |year, month, day, posts|
  - if current_year != year
    - current_year = year
    .year= year.to_s.last(2)
  %dl
    %dt= posts.first.date.strftime("%B #{countify day}")
    - for post in posts
      %dd
        %a{:href => post.url}= post.title
        