---
layout: default
title: tag index
---

.post
  %h1 Tags
  %dl
    - for (tag, pages) in site.tags.sort
      %dt{:id => CGI.escape(tag)}= h tag
      - for page in pages
        %dd= link_to_page(page)